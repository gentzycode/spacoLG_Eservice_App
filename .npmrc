# Force single React version across all dependencies
legacy-peer-deps=true

# Increase timeout for slow connections
fetch-timeout=60000

# Optimize installation
prefer-offline=false
audit=false
fund=false
